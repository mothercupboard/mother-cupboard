{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "@sentry/serverless": "^7.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "serverless": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
